/*
** EPITECH PROJECT, 2021
** test
** File description:
** main
*/

#include <stdlib.h>
#include <stdio.h>

int main(void)
{
    // invalid write of size 1

    // char *str = malloc(sizeof(char) * 10);
    // int i = 0;
    // for (i = 0; i < 15; i++)
    //     str[i] = '\0';
    // free(str);

    // invalid read of size 1
    // int i;
    // int *ptr = NULL;

    // i = *ptr;

    // conditional jump
    // int i;

    // if (i == 0)
    //     printf("Fuck\n");
    // invalid free

    return (0);
}